program: sweep_train.py
project: flow-model
method: bayes
metric:
  goal: minimize
  name: test_loss
parameters:
  latent_scalars:
    values:
      - 4
      - 8
      - 16
  latent_vectors:
    values:
      - 4
      - 8
      - 16
  latent_tensors:
    min: 0
    max: 4 
  latent_layers:
    min: 2
    max: 8 