program: sweep_train.py
project: flow-model
method: bayes
metric:
  goal: minimize
  name: val_loss
parameters:
  latent_layers:
    max: 10
    min: 1
  latent_scalars:
    max: 32
    min: 4
  latent_vectors:
    max: 16
    min: 2
  rc:
    max: 0.035
    min: 0.015